# Python If/Else and Loops Functions

This project contains Python scripts demonstrating:

- Conditional statements (`if`, `elif`, `else`)
- Random number generation
- Function usage
- Loops (for, while)

